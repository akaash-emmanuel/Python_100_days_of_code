print("Hello"[0])           #the square brackets beside the string gives the value of the index, called subscripting

print("Hello"[-1])          #-1 gives the value of the last index, (-0 doesnt make sense)

print("Hellu" + "World")    # concatenates the strings

num = 123456789              # this big number can be read by placing _ instead of commas in written language
num_readable = 123_456_789   # 123_456_789 is the same as 123,456,789

type(var)                    # this gives the type of the variable inside the parenthesis

str(var)                     # converts the variable into a string
int(var)                     #comverts the variable into an int
float(var)                   # converts the variable into a float
