#functions with outputs: allowing functions to return values after they are done executing the function block
#you can use an early return (using return keyword when catching error cases) to solve issues

#docstrings: to write comments of multiple lines, we use """   """, three quotation marks, but command + / is a better option
